<div class="container">
<h1>My Tasks</h1>
<% @tasks.each do |task|  %>
<ul>
<% if task.completed == true %>
<i class="far fa-check-square"></i>
<% else %>
<i class="far fa-square"></i>
<% end %>
<%= link_to task.title, task_path(task)%>
<%= link_to '🗑', task_path(task), method: :delete %>
</ul>
<% end %>

<%= link_to 'Create new task', task_path(:new) %>
</div>
